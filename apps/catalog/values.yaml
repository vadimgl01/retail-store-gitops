replicaCount: 1

image:
  repository: 630019796862.dkr.ecr.eu-central-1.amazonaws.com/retail-vadim-store-catalog
  pullPolicy: Always
  tag: "latest"

imagePullSecrets:
  - name: regcred

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 1000

securityContext:
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    memory: 256Mi
  requests:
    cpu: 256m
    memory: 256Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}
topologySpreadConstraints: []

metrics:
  enabled: true
  podAnnotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"

configMap:
  create: true
  name: ""

# 1. ROOT LEVEL: One of these WILL fill that empty DB_NAME line in the ConfigMap
persistence:
  provider: "mysql"
  endpoint: "catalog-mysql:3306"
  dbName: "catalog"
  databaseName: "catalog"  # <--- Added variation
  database: "catalog"      # <--- Added variation
  name: "catalog"          # <--- Added variation
# 2. APP LEVEL: This prevents the 'nil pointer' evaluations and secret crashes
app:
  persistence:
    provider: "mysql"
    endpoint: "catalog-mysql:3306"
    dbName: "catalog"
    secret:
      create: true
      name: "catalog-db"
      username: "catalog_user"
      password: "my-secret-pw"
  
  # 3. CONTAINER ENV: This is what the actual Go code reads to stop the 500
  env:
    - name: RETAIL_CATALOG_PERSISTENCE_PROVIDER
      value: "mysql"
    - name: RETAIL_CATALOG_PERSISTENCE_ENDPOINT
      value: "catalog-mysql:3306"
    - name: RETAIL_CATALOG_PERSISTENCE_DB_NAME
      value: "catalog"
    - name: RETAIL_CATALOG_PERSISTENCE_USER
      value: "catalog_user"
    - name: RETAIL_CATALOG_PERSISTENCE_PASSWORD
      value: "my-secret-pw"
    # Fallback variables for different app versions
    - name: DB_ENDPOINT
      value: "catalog-mysql:3306"
    - name: DB_PASSWORD
      value: "my-secret-pw"
    - name: DB_USER
      value: "catalog_user"
    - name: DB_NAME
      value: "catalog"
persistence:
  provider: mysql
  endpoint: "catalog-mysql:3306"
  dbName: "catalog"

mysql:
  create: true
  rootPassword: "my-secret-pw"
  mysqlUser: "catalog_user"
  mysqlPassword: "my-secret-pw"
  mysqlDatabase: "catalog"
  image:
    repository: public.ecr.aws/docker/library/mysql
    pullPolicy: IfNotPresent
    tag: "8.0"
  service:
    type: ClusterIP
    port: 3306
  persistentVolume:
    enabled: true
    storageClass: "retail-vadim-gp2"
    size: 8Gi
    accessModes:
      - ReadWriteOnce

securityGroups:
  create: false
  securityGroupIds: []

opentelemetry:
  enabled: false
  instrumentation: ""

podDisruptionBudget:
  enabled: false
  minAvailable: 2
  maxUnavailable: 1
